<p class='author'>
    @<%= comment.commenter %>:
</p>

<p class='comment'>
    <%= comment.body %>
</p>
<div class="w-100 p-end">
    <%= link_to "Delete", article_comment_path(id: comment.id, article_id: comment.article_id), method: :delete, data: {confirm: "Are you sure?"}, 'class' => 'delete del-comment' %>
</div>